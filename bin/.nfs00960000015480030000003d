#!/bin/bash
function cleanup() {
    echo "Cleaning up temporary files..."
    find ${temp_dir} -name 'pybedtools*' -type f -delete
}

trap cleanup SIGINT

# Infinite loop to keep the script running
while true; do
    sleep 1
done
